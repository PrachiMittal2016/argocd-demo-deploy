apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo
spec:
  rules:
    - host: demo.staging
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: demo-active
                port:
                  number: 8080
